== README

Recruitment Agency Site - Ruby on Rails and AngularJs Web Application

Ruby version = ruby 2.1.1

Install gemfiles:
bundle install

Install bower dependencies:
bundle exec rake bower:install

Then create database with seeds:
bundle exec rake db:reset

For test, just start:
rspec